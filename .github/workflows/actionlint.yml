name: Validate Workflows & Scripts

on:
  push:
    branches:
      - main
      - dev
    paths:
      - '.github/actions/**.yml'
      - '.github/actions/**.yaml'
      - '.github/workflows/*.yml'
      - '.github/workflows/*.yaml'
      - '.github/scripts/**'
  pull_request:
    paths:
      - '.github/actions/**.yml'
      - '.github/actions/**.yaml'
      - '.github/workflows/*.yml'
      - '.github/workflows/*.yaml'
      - '.github/scripts/**'

permissions:
  contents: read

jobs:
  actionlint:
    runs-on: ubuntu-latest
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          disable-sudo: true
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            files.pythonhosted.org:443
            github.com:443
            objects.githubusercontent.com:443
            pypi.org:443
            raw.githubusercontent.com:443
            release-assets.githubusercontent.com:443
            registry.npmjs.org:443

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Validate action metadata YAML
        shell: bash
        run: |
          git ls-files '.github/actions/*.yml' '.github/actions/*.yaml' '.github/actions/**/*.yml' '.github/actions/**/*.yaml' \
            | xargs -r ruby -e 'require "yaml"; ARGV.each { |file| YAML.load_file(file) }'

      - name: Shellcheck and unit-test scripts
        shell: bash
        run: |
          git ls-files '.github/scripts/*.sh' | xargs -r shellcheck
          bash .github/scripts/promote-main.test.sh

      - uses: ./.github/actions/actionlint
